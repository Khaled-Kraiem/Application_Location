<form method="post" action="{{ path('sous_category_delete', {'id': sous_category.id}) }}" onsubmit="return confirm('Voulez-vous vraiment effacer definitivement cette sous catégorie ?');">
	<input type="hidden" name="_method" value="DELETE">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sous_category.id) }}">
	<button class="btn">Delete</button>
</form>
